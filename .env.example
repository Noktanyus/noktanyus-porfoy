# Bu dosya, projenin ihtiyaç duyduğu ortam değişkenleri için bir şablondur.
# Gerçek değerleri içeren .env dosyasını oluşturmak için bu dosyayı kopyalayın.
# CI/CD süreçleri için bu dosyadaki sahte değerler kullanılır.

# Prisma & Veritabanı
# PostgreSQL, MySQL veya SQLite gibi bir veritabanı bağlantı adresi.
# Örnek: "postgresql://user:password@host:port/database?sslmode=prefer"
DATABASE_URL="file:./dev.db"

# NextAuth (Kimlik Doğrulama)
# Uygulamanızın tam adresi.
NEXTAUTH_URL="http://localhost:3000"
# Oturumları şifrelemek için kullanılan gizli bir anahtar.
# Üretim ortamında 'openssl rand -base64 32' komutuyla güvenli bir anahtar oluşturun.
NEXTAUTH_SECRET="super-secret-key-for-development"

# GitHub API (NextAuth veya diğer entegrasyonlar için)
# GitHub'da oluşturduğunuz OAuth uygulamasının bilgileri.
GITHUB_ID=""
GITHUB_SECRET=""

# Nodemailer (E-posta Gönderimi)
EMAIL_SERVER_HOST="smtp.example.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="user@example.com"
EMAIL_SERVER_PASSWORD="password"
EMAIL_FROM="noreply@example.com"
CONTACT_EMAIL="contact@example.com"

# Cloudflare Turnstile (CAPTCHA)
# Public site key'ler "NEXT_PUBLIC_" ile başlamalıdır.
NEXT_PUBLIC_TURNSTILE_SITE_KEY="1x00000000000000000000AA"
TURNSTILE_SECRET_KEY="1x0000000000000000000000000000000AA"

# Diğer Ayarlar
# Proje dosyalarının bulunduğu yerel Git deposunun tam yolu.
# Örnek (Linux): "/home/user/my-project"
# Örnek (Windows): "C:\Users\User\Desktop\my-project"
GIT_REPO_PATH=""
